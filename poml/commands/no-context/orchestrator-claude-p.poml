<poml>
  <role>Claude Code Three Tier Architecture POC 3層アーキテクチャ検証オーケストレーター</role>
  
  <task>
    Claude Code Three Tier Architecture POCの3層アーキテクチャを検証するため、Layer 2カスタムスラッシュコマンドを呼び出す：
    <list>
      <item>Execute zundamon-workflow using claude -p command</item>
      <item>Execute lum-workflow using claude -p command</item>
      <item>全ての実行結果を統合してTerminalに表示</item>
    </list>
  </task>
  
  <p>### Method 1: Claude -p Command Execution</p>
  <p>Using claude -p to execute custom slash commands:</p>
  <list>
    <item>1. Execute zundamon-workflow using: claude -p zundamon-workflow</item>
    <item>2. Execute lum-workflow using: claude -p lum-workflow</item>
    <item>3. Aggregate and display all results</item>
  </list>
  
  <p>Let me execute the commands using claude -p...</p>
  
  <p>実行手順：</p>
  <list>
    <item>1. Execute zundamon-workflow using claude -p command</item>
    <item>2. Execute lum-workflow using claude -p command</item>
    <item>3. 全ての実行結果を統合表示</item>
  </list>
  
  <p>重要：Layer 1 Orchestrator → Layer 2 Custom Slash Commands → Layer 3 Sub-Agents の完全な3層アーキテクチャを実証します。</p>
  
  <p>期待される結果：両方のコマンドが正常に実行され、結果が表示される</p>
  
  <example>
    <p>期待される出力形式:</p>
    <p>## 🎯 Claude Code Three Tier Architecture POC 検証結果</p>
    <p></p>
    <p>| Layer | Component | Status | Details |</p>
    <p>|-------|-----------|--------|---------|</p>
    <p>| **Layer 1** | Orchestrator | ✅ | POML読み込み・統合制御完了 |</p>
    <p>| **Layer 2** | Zundamon Command | ✅ | 2つのサブエージェント呼び出し成功 |</p>
    <p>| **Layer 2** | lum-chan Command | ✅ | 2つのサブエージェント呼び出し成功 |</p>
    <p>| **Layer 3** | zundamon-greeter | ✅ | 挨拶機能実行完了 |</p>
    <p>| **Layer 3** | zundamon-storyteller | ✅ | 物語生成機能実行完了 |</p>
    <p>| **Layer 3** | lum-greeter | ✅ | 挨拶機能実行完了 |</p>
    <p>| **Layer 3** | lum-emotion | ✅ | 感情表現機能実行完了 |</p>
    <p></p>
    <p>### 🟢 Zundamon Command 実行結果</p>
    <p></p>
    <p>#### zundamon-greeter サブエージェント出力:</p>
    <p>[サブエージェントの完全な出力をここに詳細表示]</p>
    <p></p>
    <p>#### zundamon-storyteller サブエージェント出力:</p>
    <p>[サブエージェントの完全な出力をここに詳細表示]</p>
    <p></p>
    <p>### 🟡 lum-chan Command 実行結果</p>
    <p></p>
    <p>#### lum-greeter サブエージェント出力:</p>
    <p>[サブエージェントの完全な出力をここに詳細表示]</p>
    <p></p>
    <p>#### lum-emotion サブエージェント出力:</p>
    <p>[サブエージェントの完全な出力をここに詳細表示]</p>
    <p></p>
    <p>### 🔧 技術検証結果</p>
    <p></p>
    <p>| 機能 | 実装状況 | 動作確認 |</p>
    <p>|------|----------|----------|</p>
    <p>| POML解析 | ✅ | pomljs正常動作 |</p>
    <p>| Task Tool連携 | ✅ | 4サブエージェント並列実行 |</p>
    <p>| キャラクター制御 | ✅ | Zundamon・lum-chan両対応 |</p>
    <p>| 動的エージェント呼び出し | ✅ | POML定義による動的実行 |</p>
    <p></p>
    <p>### 📊 実行統計</p>
    <p></p>
    <p>- **総サブエージェント数**: 4</p>
    <p>- **成功実行数**: 4 (100%)</p>
    <p>- **Layer間連携**: 正常</p>
    <p>- **POML統合**: 正常</p>
  </example>
</poml>